// Makes the jest-dom matchers (toBeInTheDocument, toHaveValue, ...) known to
// `tsc` during `npm run typecheck`. At runtime they are registered by the
// `vitest.setup.ts` file.
import '@testing-library/jest-dom/vitest';

