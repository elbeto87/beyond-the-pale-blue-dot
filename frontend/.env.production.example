# Production build (Vercel).
#
# Set this in the Vercel project's Environment Variables (Settings → Environment
# Variables) to the PUBLIC URL of the deployed backend. There is no dev-server
# proxy in production, so the app must call the API with an absolute URL.
#
# Example:
VITE_API_URL=https://your-backend-host.example.com

